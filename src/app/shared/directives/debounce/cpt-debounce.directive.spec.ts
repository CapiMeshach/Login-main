import { CptDebounceClickDirective } from './cpt-debounce.directive';

describe('CptDebounceDirective', () => {
  xit('should create an instance', () => {
    const directive = new CptDebounceClickDirective();

    expect(directive).toBeTruthy();
  });
});
